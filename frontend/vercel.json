{
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/exercises/(.*)", "dest": "/exercises/$1" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
